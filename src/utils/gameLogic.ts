// src/utils/gameLogic.ts
export function initBoard() {
  // return 8x8 array with initial piece positions
}

export function movePiece(board, from, to) {
  // shallow copy board and swap positions
} 